parameters:
  environment: ''

stages:
  - stage: Greetings
    displayName: Greetings
    dependsOn: []
    variables:
      - template: ../variables/${{ parameters.environment }}.yaml
    jobs:
      - job: SayHello
        displayName: Greet Everyone
        steps:
          - template: ../steps/hello-world.yml